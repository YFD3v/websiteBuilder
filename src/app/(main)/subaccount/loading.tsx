import Loading from "@/components/global/loading";

const LoadingSubAccountPage = () => {
  return (
    <div className="h-screen w-screen flex justify-center items-center">
      <Loading></Loading>
    </div>
  );
};

export default LoadingSubAccountPage;
